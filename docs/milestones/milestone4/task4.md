# Task 4: The Wizard

## Goal
Friendly first-run experience.

## Steps
1.  **First Run Detection**
    -   If `Load()` returns "no file found" error.

2.  **Welcome Message**
    -   Display welcome text in UI.
    -   "Welcome! Type /connect <host> <port> to start."

3.  **Dynamic Connection**
    -   Ensure `/connect` works even if not connected at startup.

## Verification
-   Delete config.
-   Run app.
-   Verify welcome text.
